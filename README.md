# PRADO User Meta Module

This PRADO 4.3 module extension implements Metadata for PRADO\Security\IUser.

It accounts for specific userid, roles, keys, values, and autoload.  roles and autoload are optional.
